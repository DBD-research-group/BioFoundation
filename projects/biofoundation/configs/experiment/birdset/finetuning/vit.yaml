# @package _global_
#package global is neccessary!
defaults:
  - birdset/finetuning/finetuning_config.yaml 
  - override /module/network: vit.yaml

datamodule:
  transforms:
    max_length: 5
  dataset:
    dataset_name: POW
    hf_name: POW