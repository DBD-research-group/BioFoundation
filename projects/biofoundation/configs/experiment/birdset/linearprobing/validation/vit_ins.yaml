# @package _global_
#package global is neccessary!
defaults:
  - birdset/linearprobing/validation/validation_config.yaml 
  - override /module/network: vit_ins.yaml 

datamodule:
  transforms:
    preprocessing:
      normalize_waveform: "instance_min_max"
