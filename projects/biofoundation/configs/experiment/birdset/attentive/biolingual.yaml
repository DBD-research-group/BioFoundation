# @package _global_
#package global is neccessary!
defaults:
  - birdset/attentive/attentive_config.yaml 
  - override /module/network: biolingual.yaml 
  - override /datamodule/transforms: biolingual_multilabel.yaml

module:
  network:
    model:
      preprocess_in_model: False