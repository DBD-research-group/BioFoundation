# @package _global_
#package global is neccessary!
defaults:
  - beans/restrict/restrict_config.yaml 
  - override /module/network: surfperch.yaml  

train: False

logger:
  wandb:
    name: ${module.network.model_name}_restricted_BEANS_${datamodule.dataset.dataset_name}_${seed}_${start_time}

module:
  network:
    model:
      classifier: null
      restrict_logits: True

datamodule:
  dataset:
    val_split: null
    class_weights_loss: null
    class_weights_sampler: null
    classlimit: null
    eventlimit: null