# @package _global_
#package global is neccessary!
defaults:
  - beans/attentive/attentive_config.yaml 
  - override /module/network: biolingual.yaml  
  - override /datamodule/transforms: biolingual_multiclass.yaml

module:
  network:
    model:
      preprocess_in_model: False